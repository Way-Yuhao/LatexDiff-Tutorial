% overrides preeemble to change latexdiff styles

% Change A: additions are only highlighted in blue, not underline.
\renewcommand{\DIFadd}[1]{\textcolor{blue}{#1}}  
\renewcommand{\DIFaddbegin}{}  
\renewcommand{\DIFaddend}{} 